define(['d3'],
function(d3){

/*

	var drawKeySignature = function(key, parent, startX, )
	var fifths = -5;
	var keysigX = startX;
	var keySpacing = 0.5 * spacing;
	var keysig = directions.append('svg:g')
		.attr('class', 'keysig')
		.attr('transform', 'translate(' + keysigX + ',0)');
	var flatPos = [onLine(3), onSpace(4), onSpace(2), onLine(4), onLine(2), onSpace(3), onSpace(1)];
	var sharpPos = [onLine(5), onSpace(3), onSpace(5), onLine(4), onSpace(2), onSpace(4), onLine(3)];
	var keyGlyph = fifths < 0 ? getGlyph('accidentalFlat') : getGlyph('accidentalSharp');
	for (var f = 0; f < Math.abs(fifths); f++) {
		keysig.append('svg:text')
			.attr('x', f * keySpacing)
			.attr('y', function() {
				return keyType === 'flat' ? flatPos(f) : sharpPos(f)
			})
			.text(function() {
				return keyType === 'flat' ? getGlyph('accidentalFlat') : getGlyph('accidentalSharp')
			});
	}

*/
});